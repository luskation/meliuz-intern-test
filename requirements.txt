pandas>=2.0
numpy>=1.24
scipy>=1.10

# Diferencial opcional: escrita direta no Google Sheets (ver AGENTS.md / README).
# Só é usado se GOOGLE_SHEETS_ID e GOOGLE_SHEETS_CREDENTIALS_JSON estiverem configurados;
# sem essas variáveis, o pipeline funciona normalmente sem essas duas libs.
gspread>=6.0
google-auth>=2.30
